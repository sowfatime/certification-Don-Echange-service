export class User{
        id! : string; 
		name!: string;	
		telephone !: string;	
		photo !: string;	
		email !: string; 
		email_verified_at !: string;	
		password !: string;	
		is_bloqued !: string;	
		role_id !: string;	
		remember_token !: string;	
	created_at !: string;		
		updated_at !: string;
}